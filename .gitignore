# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example

# generated QRZ fetch working files
.cache/

# python bytecode from running scripts/ and tests/
__pycache__/
*.py[cod]

# scripts/check-widths.js needs Playwright, which is installed on demand and
# never committed — the site itself has no npm dependencies.
node_modules/
package-lock.json
